# TeamTalk AI 协作约定

本文件只规定所有 AI 开发任务都必须遵守的工作方式。产品、架构、协议、部署和测试知识从
[文档中心](doc/README.md)进入，不在这里重复维护。

## 项目边界

- 使用中文沟通，先说明结论、证据和风险。
- 当前 `team-talk/` 是核心仓库；同级目录只用于研究和对照，未经明确要求不得修改。
- 项目尚未正式发布，允许有理由的破坏性重构和清理测试数据，但必须说明影响并完成验证。
- Git 提交是 AI 开发过程的工作快照。完成一个可验证阶段后主动提交，正式发布前再整理历史。
- 工作区可能包含其他人的改动；先查看状态，只修改任务相关内容，不覆盖或回退未知变更。

## 开始工作

1. 阅读根 [README](README.md)、[文档中心](doc/README.md)和任务所属章节。
2. 用当前代码和测试核对事实，不把历史注释、截图或旧文档直接当作现行设计。
3. 明确数据所有者和完整链路；涉及协议、SDK、客户端与服务端时不能只修其中一层。
4. 先确认[功能状态](doc/10-reference/feature-status.md)，不要把计划能力当成已经实现。

## 核心约束

- 依赖保持单向：`android / desktop → app → shared → protocol`，`server → protocol`。
- `shared` 是完整 IM SDK，不依赖 Compose；SDK 缺陷在 SDK 层闭环，不能由 UI 绕过。
- `protocol` 只放跨端契约和纯规则，不得依赖客户端 SDK、UI 或服务端基础设施。
- UI 观察 LocalCache，RPC 与 Notify 负责使缓存收敛；服务端是权限和共享数据的权威来源。
- 状态和资源遵循单一所有者；短暂断线不能清除用户身份，认证终态失败必须停止重连。
- RPC methodId、NotifyType、MessageType 和 wire 字段是兼容性契约；修改时遵循
  [协议规则](doc/04-protocol/README.md)并补 round-trip 测试。
- 文件消息只保存 TeamTalk FileStore 相对路径。服务器必须在分配序号和成功 ACK 前确认附件及
  元数据真实存在；ImBot 和图形客户端不得绕过同一校验链。
- Desktop 与 Android 共享产品语义和设计令牌，不强行共享导航、窗口和平台交互。
- 私有化部署参数必须可配置；秘密不得提交，临时需求不得演化为大量 profile、flavor 或开关。

详细规则见[系统架构](doc/03-architecture/README.md)、[工程约束](doc/08-development/engineering-rules.md)
和[变更指南](doc/08-development/change-guides.md)。

## 实施与安全

- 诊断任务只分析和报告；用户要求修改时才实施修复。
- 不使用破坏性 Git 命令，不删除范围不明的数据，不修改参考仓库。
- 不使用 `pkill -f gradle` 停止任务；只结束自己启动的会话或已确认 PID。
- 生产代码不使用 `println` 记录业务日志；日志不得包含密码、token、私钥或完整敏感正文。
- 不用演示数据、假成功或空实现伪装尚未完成的业务能力。
- 用户可见界面改动必须运行真实客户端，并通过语义操作和截图核对关键状态。

## 验证与提交

- 按风险选择共享模块测试、服务端测试、客户端编译和真实部署验收。
- 跨账户、数据库、文件与客户端的业务流程优先进入真实部署验收；本地单测保护确定性边界。
- 协议、配置、状态或交互发生变化时，同步更新对应权威文档和功能状态。
- 提交前查看完整 diff，执行 `git diff --check`，确认没有无关改动后创建清晰的 Git 提交。

测试入口见[测试与验收](doc/09-testing/README.md)，Desktop 操作方式见
[Desktop 自动化](doc/09-testing/desktop-automation.md)。

## 文档维护

- 按稳定知识领域修改现有章节，不为每个需求、阶段或故障新建顶级目录。
- 架构设计、操作手册、参考表和路线图分开维护；临时过程留在任务、提交或 issue。
- 移动文件后扫描 Markdown、源码注释和本文件中的旧链接。
- 代码与文档冲突时先核实权威来源，再同时修正文档，不能长期保留已知错误。
